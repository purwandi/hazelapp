type Project {
  id: ID!
  name: String!
  description: String
  createdAt: String!
}

type ProjectConnection {
  totalCount: Int!
  edges: [ProjectEdge]
}

type ProjectEdge{
  cursor: ID!
  node: Project
}
