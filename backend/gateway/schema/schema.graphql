schema {
  query: Query
  mutation: Mutation
}

type Query {
  projects(): ProjectConnection!
  project(id: ID!): Project!
}

type Mutation {
  ProjectCreate(name: String!, description: String): Project
  IssueCreate(title: String!, body: String!, project_id: ID!): Issue
}
