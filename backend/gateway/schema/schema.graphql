schema {
  query: Query
  mutation: Mutation
}

type Query {
  viewer(): User!
  # projects(): ProjectConnection!
  # project(id: ID!): Project!
  # milestones(input: GetMilestoneQueryInput) [Milestone]
}

type Mutation {
  ProjectCreate(input: CreateProjectInput!): Project
  # IssueCreate(input: CreateIssueInput!): Issue
  # IssueUpdate(input: UpdateIssueInput!): Issue
  # MilestoneCreate(input: CreateMilestoneInput!): Milestone
}
